
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-offset-sm-3 col-sm-6">
      <h1>Restaurants</h1>
      <ul>
        <% @restaurants.each do |r| %>
        <li>
          <%= link_to r.name , restaurant_path(r) %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</div>

